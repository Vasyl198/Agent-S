name: ML Dataset Report

'on':
  push:
    branches: [ "main", "ci/*" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch: {}

jobs:
  report:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        working-directory: Agent-S-main
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || true
          pip install numpy pandas scikit-learn
          # важно: экспорт в ЭТОМ шаге
          export PYTHONPATH="$PYTHONPATH:$GITHUB_WORKSPACE/Agent-S-main"
          python -c "import agent.fashion.ml.dataset_report; print('OK: import works')"

      - name: Generate ML artifacts (bundle)
        working-directory: Agent-S-main
        run: |
          export PYTHONPATH="$PYTHONPATH:$GITHUB_WORKSPACE/Agent-S-main"
          mkdir -p artifacts/ml
          python -m agent.fashion.ml.dataset_report \
            --dataset tests/fixtures/runs_mixed.jsonl \
            --check --min-runs 2 \
            --bundle --out-dir artifacts/ml \
            --stdout

      - name: Upload artifacts (private)
        uses: actions/upload-artifact@v4
        with:
          name: ml-artifacts
          path: Agent-S-main/artifacts/ml/
          if-no-files-found: error
